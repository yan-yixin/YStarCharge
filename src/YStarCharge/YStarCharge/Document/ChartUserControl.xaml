<UserControl x:Class="YStarCharge.Document.ChartUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YStarCharge.Document"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource AppForeground}"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="FontSize" Value="14"/>
            <EventSetter Event="Checked" Handler="Radio_Checked"/>
        </Style>
    </UserControl.Resources>
    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <RadioButton x:Name="lineRadioButton" Content="折线图" IsChecked="True"/>
            <RadioButton Content="柱状图"/>
            <RadioButton Content="饼形图"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <lvc:CartesianChart Name="lineChart"
                            Series="{Binding LineChartSerise}"
                            LegendLocation="Bottom"
                            Foreground="{StaticResource TextBlockForeground}">
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis MinValue="0" LabelFormatter="{Binding Formatter}">
                        <lvc:Axis.Separator >
                            <lvc:Separator Visibility="Hidden" StrokeThickness="0" StrokeDashArray="0" Stroke="#404F56"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisY>
                <lvc:CartesianChart.AxisX >
                    <lvc:Axis Labels="{Binding AxisXLabel}"  >
                        <lvc:Axis.Separator >
                            <lvc:Separator Visibility="Hidden" StrokeThickness="0" StrokeDashArray="0" Stroke="#404F56"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
            </lvc:CartesianChart>
            <lvc:CartesianChart Name="columnsChart"
                            Series="{Binding CloumnsChartSerise}"
                            LegendLocation="Bottom"
                            Foreground="{StaticResource TextBlockForeground}">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Labels="{Binding AxisXLabel}" >
                        <lvc:Axis.Separator >
                            <lvc:Separator Visibility="Hidden" StrokeThickness="0" StrokeDashArray="0" Stroke="#404F56"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis MinValue="0" LabelFormatter="{Binding Formatter}">
                        <lvc:Axis.Separator >
                            <lvc:Separator Visibility="Hidden" StrokeThickness="0" StrokeDashArray="0" Stroke="#404F56"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
            <lvc:PieChart Name="pieChart" LegendLocation="Bottom"
                      Series="{Binding PieChartSeries}"
                      Hoverable="False" 
                      Foreground="{StaticResource TextBlockForeground}">
                <lvc:PieChart.DataTooltip>
                    <lvc:DefaultTooltip BulletSize="10"></lvc:DefaultTooltip>
                </lvc:PieChart.DataTooltip>
            </lvc:PieChart>
        </Grid>
    </Grid>
</UserControl>
